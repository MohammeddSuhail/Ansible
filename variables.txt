---
- name: "checking variables"
  hosts: myHosts

  vars:
    favcolor: blue

  tasks:

  - name: "something"
    debug: 
      msg: '{{vars.favcolor}}'







other one:

set_fact: 
   variable_name: value

ex:
set_fact: 
   rec: "{{item}}"

here rec is the variable and it will bw assigned a value item








---

- name: "checking variables"
  hosts: myHosts

  vars:
    some: 
      some1: "a"
      some2: "b"

  tasks:

  - name: "something"
    debug: 
      msg: 'some: {{item}}'
    with_items: '{{some}}'


